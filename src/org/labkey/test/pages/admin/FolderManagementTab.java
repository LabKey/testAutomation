package org.labkey.test.pages.admin;

import org.openqa.selenium.WebDriver;

// Wave of the future.
public interface FolderManagementTab
{
    void setDriver(WebDriver driver);
    String getTabId();
}
