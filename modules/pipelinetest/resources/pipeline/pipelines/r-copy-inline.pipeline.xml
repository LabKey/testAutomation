<pipeline xmlns="http://labkey.org/pipeline/xml" name="r-copy-inline" version="0.0">
    <description>Use R to duplicate a file and generate xar exp run (inline script)</description>
    <analyzeURL>/pipelinetest/rinline.view</analyzeURL>
    <tasks>
        <taskref ref="pipelinetest:task:r-copy-inline"/>
        <taskref ref="org.labkey.api.exp.pipeline.XarGeneratorId"/>
    </tasks>
</pipeline>
